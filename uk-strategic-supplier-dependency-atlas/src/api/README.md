# src/api

Intentionally empty for the alpha. The static-alpha interface rule
(mission section 38) requires static generated pages with no runtime
database; a local API is only added if static pages become insufficient
after the mini cohort. Do not build here before that evidence exists.
